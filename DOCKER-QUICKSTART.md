# 🐳 Docker Quick Start Guide

Быстрая инструкция для локального тестирования перед деплоем на VPS.

---

## 📋 Требования

- Docker Desktop установлен и запущен
- Git (для клонирования)

---

## 🚀 Локальный запуск

### 1. Подготовка .env

```bash
# Копируем пример
copy .env.example .env

# Редактируем .env (notepad, VS Code, любой редактор)
notepad .env
```

Заполните обязательные переменные:
```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
WORKSECTION_DOMAIN=your-company.worksection.com
WORKSECTION_HASH=your_api_key_here
```

### 2. Сборка и запуск

```bash
# Сборка образа и запуск контейнера
docker compose up -d --build

# Проверка статуса
docker compose ps

# Должен быть статус: Up (healthy)
```

### 3. Просмотр логов

```bash
# Логи в реальном времени
docker compose logs -f

# Последние 50 строк
docker compose logs --tail=50

# Выход: Ctrl+C
```

### 4. Проверка работы

```bash
# Health check
curl http://localhost:3001/api/health

# Расписание планировщика
curl http://localhost:3001/api/schedule

# Ручной запуск синхронизации
curl -X POST http://localhost:3001/api/sync
```

---

## 🔄 Управление

```bash
# Остановка
docker compose stop

# Запуск
docker compose start

# Перезапуск
docker compose restart

# Полная остановка и удаление
docker compose down

# Пересборка после изменений в коде
docker compose down
docker compose up -d --build
```

---

## 🔍 Диагностика

### Проверка логов планировщика

```bash
# Фильтр по ключевым словам
docker compose logs | findstr "Планировщик"
docker compose logs | findstr "синхронизац"
docker compose logs | findstr "ERROR"
```

### Проверка что планировщик запустился

В логах должно быть:
```
⏰ Инициализация планировщика автоматической синхронизации...
📅 Расписание: 0:00, 3:00, 6:00, 9:00, 12:00, 15:00, 18:00, 21:00 (Europe/Minsk)
✅ Задача создана: синхронизация каждый день в 0:00
✅ Задача создана: синхронизация каждый день в 3:00
...
✨ Планировщик инициализирован! Автоматическая синхронизация активна.
```

### Проверка автоматической синхронизации

Дождитесь следующего запланированного времени (кратного 3 часам) и проверьте логи:
```bash
docker compose logs -f | findstr "Запуск автоматической"
```

---

## 📊 Мониторинг

```bash
# Использование ресурсов
docker stats ws-to-work

# Детальная информация о контейнере
docker inspect ws-to-work

# Проверка healthcheck
docker inspect ws-to-work | findstr "Health"
```

---

## ❗ Troubleshooting

### Контейнер не запускается

```bash
# Проверяем логи
docker compose logs

# Проверяем .env
type .env

# Пересоздаем контейнер
docker compose down
docker compose up -d --build --force-recreate
```

### Порт 3001 занят

Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "3002:3001"  # локальный порт 3002 → контейнер порт 3001
```

### Синхронизация не запускается

1. Проверьте что планировщик инициализирован (см. логи выше)
2. Проверьте что сейчас не выходной
3. Проверьте что час кратен 3 (0, 3, 6, 9, 12, 15, 18, 21)
4. Запустите вручную: `curl -X POST http://localhost:3001/api/sync`

---

## ✅ Готово к деплою?

Если локально всё работает:

1. ✅ Контейнер запускается без ошибок
2. ✅ Healthcheck проходит
3. ✅ Планировщик инициализируется
4. ✅ Ручная синхронизация работает
5. ✅ Telegram уведомления приходят (если настроены)

**→ Можно деплоить на VPS! См. DEPLOY.md**

---

## 🧹 Очистка

После тестирования:

```bash
# Остановить и удалить контейнер
docker compose down

# Удалить образ (опционально)
docker rmi ws-to-work-ws-to-work-sync

# Очистить все неиспользуемые образы Docker
docker system prune -a
```

---

**Готово! Теперь можно переносить на VPS.** 🚀
